# Local development — backend runs on port 5000
VITE_API_URL=http://localhost:5000/api

# Production (Render backend) — used by frontend/.env.production:
# VITE_API_URL=https://ai-recommendation-engine-2.onrender.com/api
